import {
  Cart,
  Category,
  DiscountInfo,
  Product,
  Showroom,
  SideBarContent,
} from '../interfaces';

export const initGallerySrc: string[] = [];

export const initCart: Cart = [];

export const initDiscountInfo: DiscountInfo | null = null;

export const initShowrooms: Showroom[] = [];

export const initProduct: Product | null = null;

export const initCategories: Category[] = [];

export const initSideBarContent: SideBarContent[] | null = null;

export const initProducts: Product[] = [];
